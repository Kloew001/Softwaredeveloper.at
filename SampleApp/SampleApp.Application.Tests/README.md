```markdown
# SampleApp.Application.Tests

Unit-Tests (`.NET 8`) für `SampleApp.Application`.

## Test-Framework

- NUnit

## Konfiguration

Die `appsettings.json` und `appsettings.Development.json` werden aus `SampleApp.Server` verlinkt und in das Output-Verzeichnis kopiert.

## Ausführen

- Visual Studio Test Explorer
- oder `dotnet test`

## Projektabhängigkeiten

- `SampleApp.Application`
- `Infrastructure.Core.Tests`

## Repository

- https://github.com/Kloew001/Softwaredeveloper.at
```